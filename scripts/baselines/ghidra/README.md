# Usage
```bash
{GHIDRA_ROOT}/support/analyzeHeadless {PROJECT_DIR} {PROJECT_NAME} -deleteproject -import {DIR/FILE} -max-cpu {CORES} -recursive -postScript baselines/ghidra/disassemble.py {BASE_DIR} {OUTPUT_DIR}
```